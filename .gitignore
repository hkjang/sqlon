bin/
dist/
logs/
data/*/feedback/
data/*/audit/
data/*/backups/
data/*/learned_rules.json
jamypg-mcp-linux
jamypg-mcp-*.exe
# Local Go build artifacts. Release binaries belong in CI artifacts, never in
# the source repository.
/jamypg-mcp
/sqlon
/sqlon_test_bin
data/*/metasync/
data/*/reviews/
data/*/openmetadata.json
data/*/profiles/
# Local tooling / IDE / generated media — not part of the source repository.
output/
.gitframe/
.claude/

# 실행 중 만들어지는 로컬 데이터 — data/<프로필>/ 아래 몇몇은 이미 무시하고
# 있었지만 프로필 디렉터리 자체와 백업은 빠져 있었다.
/data/backups/
/data/sqlon/

# 데모 서버를 띄우는 로컬 도우미 (경로가 이 기계에 묶여 있다)
/scripts/start_demo_servers.sh
